logging:
    level:
        org:
            springframework:
                data:
                    mongodb:
                        core:
                            ReactiveMongoTemplate: DEBUG
        root: info
    pattern:
        console: '%d{dd-MM-yyyy HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level)
            %logger.%M - %msg%n'
spring:
    data:
        mongodb:
            uri: mongodb+srv://project:0daVc99FvCic3c4k@cluster0.hocxk.mongodb.net/myFirstDatabase?retryWrites=true&w=majority

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true